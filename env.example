# 醒美闪图 - Docker 环境变量配置示例
# 
# 重要安全提示：
# 1. 复制此文件为 .env 并填写实际值: cp env.example .env
# 2. .env 文件已在 .gitignore 中，不会被提交到代码仓库
# 3. 生产环境必须使用强密钥，不要使用示例值
# 4. 定期轮换密钥，尤其是在密钥可能泄露后
#
# 生成安全随机密钥的命令：
#   openssl rand -base64 32

# ==================== 微信小程序配置 (必填) ====================
WX_APPID=your_wx_appid
WX_SECRET=your_wx_secret

# ==================== 微信支付配置 (可选) ====================
WX_MCH_ID=your_mch_id
WX_PAY_API_KEY=your_pay_api_key
WX_PAY_CERT_SERIAL=your_cert_serial
WX_PAY_NOTIFY_URL=https://your-domain.com/api/pay/notify

# ==================== AI 服务配置 (必填) ====================
AI_API_KEY=your_ai_api_key
AI_API_BASE=https://api.vectorengine.ai

# ==================== 腾讯云 COS 配置 (必填) ====================
# 用于存储用户照片和素材
# 获取方式: 腾讯云控制台 -> 访问管理 -> 访问密钥 -> API密钥管理
COS_SECRET_ID=your_cos_secret_id
COS_SECRET_KEY=your_cos_secret_key

# ==================== JWT 配置 (必填) ====================
# 用于后台管理系统认证，必须设置为强随机值
# 生成命令: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING

# ==================== 数据库配置 ====================
# SQLite 数据库路径 (Docker 卷内，一般不需要修改)
SHARED_DB_PATH=/app/data/flashphoto.db

# ==================== 部署 Webhook 配置 ====================
# 用于 GitHub Actions 触发部署，必须设置为强随机值
# 生成命令: openssl rand -base64 32
# 同时需要在 GitHub 仓库 Settings -> Secrets 中添加 WEBHOOK_SECRET
WEBHOOK_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING
