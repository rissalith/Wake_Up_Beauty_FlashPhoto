# 醒美闪图 - 通用环境配置示例
#
# 安全提示：
# 1. 复制此文件为 common.env 并填入实际值
# 2. common.env 已在 .gitignore 中，不会被提交
# 3. 生产环境必须使用强密钥
# 4. 定期轮换密钥

# ==================== 运行环境 ====================
NODE_ENV=production

# ==================== 数据库配置 ====================
# 所有服务共享统一的 SQLite 数据库
# Docker 环境固定使用 /app/data/flashphoto.db
SHARED_DB_PATH=/app/data/flashphoto.db
DB_PATH=/app/data/flashphoto.db

# ==================== 微信小程序配置 (必填) ====================
WX_APPID=your_appid_here
WX_SECRET=your_secret_here

# ==================== JWT 配置 (必填) ====================
# 用于后台管理认证，必须设置为强随机值
# 生成命令: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_SECURE_RANDOM_STRING

# ==================== 管理员配置 ====================
# 首次部署时使用，建议部署后立即修改
ADMIN_PASSWORD=change-this-password

# ==================== 腾讯云 COS 配置 (必填) ====================
# 用于存储用户照片、素材和机器翻译服务
COS_SECRET_ID=your_cos_secret_id
COS_SECRET_KEY=your_cos_secret_key

# ==================== AI 服务配置 (必填) ====================
AI_API_KEY=your_ai_api_key
AI_API_BASE=https://api.vectorengine.ai

# ==================== 微信支付配置 (可选) ====================
WX_MCH_ID=your_mch_id
WX_PAY_API_KEY=your_api_key
WX_PAY_CERT_SERIAL=your_cert_serial
# 微信支付私钥路径 (Docker 容器内路径，证书文件放在 docker/certs/ 目录)
WX_PAY_PRIVATE_KEY_PATH=/app/certs/apiclient_key.pem
WX_PAY_NOTIFY_URL=https://your-domain.com/api/pay/notify
